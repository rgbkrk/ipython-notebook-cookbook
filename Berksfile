#!/usr/bin/env ruby

site :opscode

# Locking version of the python cookbook that uses setuptools instead of distribute
cookbook 'python',
   :git => 'https://github.com/opscode-cookbooks/python.git',
   :ref => '488d6531d462d5c38bb7f05c0213f3cd694f3802'
cookbook 'apt'
cookbook 'yum'

metadata
